# Data directory (user data, temporary files, state files)
data/

# Plugin lock file changes
lazy-lock.json

# Temporary and cache files
*.tmp
*.cache
.DS_Store
# draw.io temporary/backup files
.$*

# lazy の ladder_viewer build フックが生成 (ソース: github:totochi-2022/ladder_viewer)
vivify/scripts/ladder.glue.js
